{
  "name": "cloudflare-image-host",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --live-reload --persist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}

